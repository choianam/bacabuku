<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bacabuku">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LainnyaActivity"></activity>
        <activity android:name=".UmumActivity" />
        <activity android:name=".BahasaActivity" />
        <activity android:name=".IpsActivity" />
        <activity android:name=".IpaActivity" />
        <activity android:name=".LibraryActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".menu" />
        <activity android:name=".SudahLogin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>